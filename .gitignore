# Dedup state (local only, regenerated on each run)
scripts/.seen_source_ids.json

# Fetched feed data
sources_feed.json
fetch-output.json
fetch-stderr.log

# Dependencies
node_modules/

# Build output
dist/

# Environment (secrets)
.env
